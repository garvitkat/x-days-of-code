#include<bits/stdc++.h>
using namespace std;
void solve()
{

    // if ((D >= 1 && D <= 10) && (d >= 1 && d <= 5) && (A < B) && (B < C) && A >= 1) {
    // }
    int personsAgeGroup, vaccinesperday;
    int arr[10];
    cin >> personsAgeGroup >> vaccinesperday;
    for (int i = 0; i < 10; i++) {
        cin >> arr[i];
    }


}
int main()
{

    int t;
    cin >> t;
    if ((t >= 1 && t <= 10000) )
    {
        while (t--)
        {
            solve();
            cout << endl;
        }
    }


}